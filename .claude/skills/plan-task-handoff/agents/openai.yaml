interface:
  display_name: "Plan Task Handoff"
  short_description: "Create task DAGs, handoffs, and lock scopes"
policy:
  invocation_surface: support_only
  allow_implicit_invocation: true
  may_own_execution: false
