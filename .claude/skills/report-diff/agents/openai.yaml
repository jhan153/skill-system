interface:
  display_name: "Report Diff"
  short_description: "Show verified changed lines in Canvas HTML"
  default_prompt: "Use $report-diff to present verified changed lines by intent as Report Canvas compare HTML."

policy:
  invocation_surface: support_only
  allow_implicit_invocation: false
  may_own_execution: false
  may_write: true
