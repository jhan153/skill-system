interface:
  display_name: "Knowledge Domain Record"
  short_description: "Record durable project domain knowledge"
  default_prompt: "Use $knowledge-domain-record to classify overlap before updating an existing identity or recording one new project-specific domain rule with navigation and history."
policy:
  invocation_surface: explicit_procedure
  allow_implicit_invocation: false
  may_own_execution: true
  may_write: true
  may_block_completion: false
