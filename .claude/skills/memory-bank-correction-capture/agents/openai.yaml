interface:
  display_name: "Memory Bank Correction Capture"
  short_description: "Capture persistent corrections as candidate mistakes"
  default_prompt: "Use $memory-bank-correction-capture to record project-level recurring corrections as candidate mistake memory with masked evidence."

policy:
  allow_implicit_invocation: false
