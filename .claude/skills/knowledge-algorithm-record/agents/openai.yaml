interface:
  display_name: "Knowledge Algorithm Record"
  short_description: "Record selected project algorithms"
  default_prompt: "Use $knowledge-algorithm-record to classify overlap before recording one accepted algorithm identity with semantic history, typed relations, code anchors, and verifier evidence."
policy:
  invocation_surface: explicit_procedure
  allow_implicit_invocation: false
  may_own_execution: true
  may_write: true
  may_block_completion: false
