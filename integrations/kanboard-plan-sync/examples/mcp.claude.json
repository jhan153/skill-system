{
  "_comment": "Claude Code MCP registration. Copy the 'kanboard-plan-sync' entry into your project .mcp.json (or ~/.claude.json mcpServers). The API token is referenced via ${KANBOARD_API_TOKEN} so the secret is read from the environment and never written into config.",
  "mcpServers": {
    "kanboard-plan-sync": {
      "command": "python",
      "args": ["-m", "kanboard_plan_sync_mcp"],
      "env": {
        "PYTHONPATH": "${HOME}/.ai/infra/kanboard-plan-sync/src",
        "KANBOARD_API_TOKEN": "${KANBOARD_API_TOKEN}"
      }
    }
  }
}
