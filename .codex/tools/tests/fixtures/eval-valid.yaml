version: "7.2.7"
purpose: "Valid eval schema fixture."
cases:
  - case_id: fixture-eval-valid-001
    schema_version: 2
    user_request: "수정 후 최신 검증 결과와 함께 보고해줘."
    expected_primary_skill: workflow-rigor
    expected_supporting_skills: [workflow-validation]
    should_not_trigger: [report-qualitative]
    expected_context: [changed_files, validation_command]
    expected_output_shape: "verified task result"
    quality_notes: "Completion requires fresh validation evidence."
    friction_risk: "claiming success without execution"
    expected_behaviors:
      - run_fresh_validation_before_completion
    forbidden_behaviors:
      - claim_success_without_execution
    required_evidence:
      - type: command_exit
        expected: 0
    source_feedback_ids: []
