# Merge this reviewer choice only when automatic approval review is explicitly requested and the
# effective approval policy is on-request or granular. Preserve sandbox, provider, auth, and
# unrelated settings; managed allowed_approvals_reviewers must permit auto_review when declared.
approvals_reviewer = "auto_review"
