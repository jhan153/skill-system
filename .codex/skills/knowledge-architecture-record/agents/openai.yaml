interface:
  display_name: "Knowledge Architecture Record"
  short_description: "Record project architecture boundaries"
  default_prompt: "Use $knowledge-architecture-record to classify overlap before recording one accepted architecture identity with typed dependency/scope links, history, and source anchors."
policy:
  invocation_surface: explicit_procedure
  allow_implicit_invocation: false
  may_own_execution: true
  may_write: true
  may_block_completion: false
