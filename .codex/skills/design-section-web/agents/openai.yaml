interface:
  display_name: "Design Section Web"
  short_description: "Guide section-based web implementation."
  default_prompt: "Use $design-section-web to apply section-based web constraints for hero, semantic sections, CTA flow, responsive order, and accessibility."
policy:
  allow_implicit_invocation: false
