interface:
  display_name: "Search Deep Evidence"
  short_description: "Cross-check claims across independent evidence lanes"
  default_prompt: "Use $search-deep-evidence for explicit multi-lane fact-checking. Separate source identity from claim relation, preserve contradictions, assess independence, and stop at a traceable evidence set."

policy:
  invocation_surface: explicit_procedure
  allow_implicit_invocation: false
  may_own_execution: true
