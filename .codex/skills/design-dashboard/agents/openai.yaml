interface:
  display_name: "Design Dashboard"
  short_description: "Guide dense dashboard implementation."
  default_prompt: "Use $design-dashboard to apply dashboard UI constraints for KPIs, filters, charts, tables, density, async states, and accessibility."
policy:
  allow_implicit_invocation: false
