interface:
  display_name: "Knowledge Base Read"
  short_description: "Read bounded Knowledge paths and current rules"
  default_prompt: "Use $knowledge-base-read to load the smallest current record slice and traverse only the typed why/history/scope/recurrence path needed by the task."
policy:
  invocation_surface: support_only
  allow_implicit_invocation: true
  may_own_execution: false
  may_write: false
  may_block_completion: false
