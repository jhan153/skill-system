interface:
  display_name: "Knowledge Code Review Record"
  short_description: "Record recurring repository review rules"
  default_prompt: "Use $knowledge-code-review-record to separate dependent repetition from real recurrence, then update an existing rule or record one new repository-specific review identity."
policy:
  invocation_surface: explicit_procedure
  allow_implicit_invocation: false
  may_own_execution: true
  may_write: true
  may_block_completion: false
