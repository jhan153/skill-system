interface:
  display_name: "Knowledge Algorithm Record"
  short_description: "Record selected project algorithms"
  default_prompt: "Use $knowledge-algorithm-record to record this accepted algorithm choice and its code and verifier anchors."
policy:
  invocation_surface: explicit_procedure
  allow_implicit_invocation: false
  may_own_execution: true
  may_write: true
  may_block_completion: false
