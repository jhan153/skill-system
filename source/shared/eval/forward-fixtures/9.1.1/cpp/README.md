# C++ structural-evidence fixture

This repository intentionally has CMake and include/entrypoint hints but no compilation-aware symbol/class/call index. Generate the normal `analysis-codebase` report and report the quality gate exactly; do not reinterpret file-level include/build coupling as semantic structure evidence.
