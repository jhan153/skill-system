interface:
  display_name: "Project Context Init"
  short_description: "Initialize a repository context-location manifest"
  default_prompt: "Use $project-context-init to create a minimal project-context.yaml without initializing any stores."
policy:
  invocation_surface: explicit_procedure
  allow_implicit_invocation: false
  may_own_execution: true
  may_write: true
  may_block_completion: false
