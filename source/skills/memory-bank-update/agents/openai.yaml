interface:
  display_name: "Memory Bank Update"
  short_description: "Update stored goals or rules with ledger events"
  default_prompt: "Use $memory-bank-update to mutate persistent goals or rules with append-only events and current-state reflection."

policy:
  invocation_surface: explicit_procedure
  allow_implicit_invocation: false
  may_own_execution: true
