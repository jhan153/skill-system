interface:
  display_name: "LLM Wiki Context"
  short_description: "Build context from one selected LLM Wiki"
  default_prompt: "Use $llm-wiki-context to build task context from this explicitly selected Wiki."
policy:
  allow_implicit_invocation: false
