interface:
  display_name: "Plan Long Term Package"
  short_description: "Agentic HLD alternative for phase/package planning"
  brand_color: "#2563EB"
  default_prompt: "Use $plan-long-term-package to ingest existing reports and create a validated phase/group planning package for this task. Treat the result as the Agentic HLD alternative and, when lifecycle artifacts are requested, as a delivery-architecture-package source."

policy:
  allow_implicit_invocation: false
