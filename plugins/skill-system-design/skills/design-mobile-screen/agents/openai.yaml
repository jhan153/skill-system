interface:
  display_name: "Design Mobile Screen"
  short_description: "Guide mobile screen implementation constraints."
  default_prompt: "Use $design-mobile-screen to apply mobile screen constraints for safe areas, navigation, keyboard overlays, touch targets, scrolling, states, and accessibility."
policy:
  allow_implicit_invocation: false
