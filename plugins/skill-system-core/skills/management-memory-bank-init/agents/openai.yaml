interface:
  display_name: "Management Memory Bank Init"
  short_description: "Initialize one readable project Memory file"
  default_prompt: "Use $management-memory-bank-init to create one empty canonical memory.md and register its approved root without inferred records or legacy ledgers."

policy:
  allow_implicit_invocation: false
