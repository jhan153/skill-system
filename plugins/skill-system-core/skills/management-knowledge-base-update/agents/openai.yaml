interface:
  display_name: "Management Knowledge Base Update"
  short_description: "Observe or revise existing project knowledge"
  default_prompt: "Use $management-knowledge-base-update to classify overlap, then observe, amend, reverify, supersede, deprecate, or relink one existing record with provenance and semantic history."
policy:
  allow_implicit_invocation: false
