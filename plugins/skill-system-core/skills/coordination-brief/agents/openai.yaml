interface:
  display_name: "Coordination Brief"
  short_description: "Create lightweight response-first goal briefs or task DAGs only on explicit coordination requests."
policy:
  allow_implicit_invocation: false
