interface:
  display_name: "Management Knowledge Base Read"
  short_description: "Read bounded Knowledge paths and current rules"
  default_prompt: "Use $management-knowledge-base-read to load the smallest current record slice and traverse only the typed why/history/scope/recurrence path needed by the task."
policy:
  allow_implicit_invocation: true
