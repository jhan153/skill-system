interface:
  display_name: "Management Memory Bank Update"
  short_description: "Update one durable Memory record and revision"
  default_prompt: "Use $management-memory-bank-update to change one explicitly persistent goal, rule, practice, or mistake candidate in memory.md and read back only that record and revision."

policy:
  allow_implicit_invocation: false
