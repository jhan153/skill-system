interface:
  display_name: "Project Context Checkpoint"
  short_description: "Record durable context at commit or closeout"
  default_prompt: "Use $project-context-checkpoint to capture only durable project context from this commit or closeout."
policy:
  allow_implicit_invocation: true
