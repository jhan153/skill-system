interface:
  display_name: "Project Context Init"
  short_description: "Initialize, bootstrap, or diagnose project context"
  default_prompt: "Use $project-context-init only for an explicit manifest-init, guided bootstrap, or read-only doctor request. Propose exact path and store action IDs, accept all or a subset in one transaction approval, and delegate only approved store creation without asking again unless scope changes."
policy:
  allow_implicit_invocation: false
