interface:
  display_name: "Report Implementation Explainer"
  short_description: "Explain implementation paths or verified changes"
  default_prompt: "Use $report-implementation-explainer to explain this implementation or verified change set in content-first Markdown, adding the same trace or comparison as HTML only when I request it."

policy:
  allow_implicit_invocation: false
