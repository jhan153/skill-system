interface:
  display_name: "Plan Execution Handoff"
  short_description: "Create governed Plan and Handoff execution pairs"
  default_prompt: "Use $plan-execution-handoff to create a plan-handoff-v6 typed DAG, consume Core execution-item cards without ceding Plan transitions to workers, and record advisory worker_done timing. Most plans use ordinary phase delivery; load repeated-work principles only for an admitted verifier-steered graph, and terminate phase_gate_delivery at human_test_ready."

policy:
  allow_implicit_invocation: false
