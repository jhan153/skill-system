interface:
  display_name: "Knowledge Base Read"
  short_description: "Read task-relevant project knowledge"
  default_prompt: "Use $knowledge-base-read to load only the local Knowledge records relevant to this task."
policy:
  allow_implicit_invocation: true
