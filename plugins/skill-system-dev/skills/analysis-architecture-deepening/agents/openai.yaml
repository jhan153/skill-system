interface:
  display_name: "Analysis Architecture Deepening"
  short_description: "Find ranked deep-module and architecture improvement candidates"
  default_prompt: "Use $analysis-architecture-deepening to scan a scoped codebase area for architecture friction and return ranked deep-module, seam, adapter, shallow-wrapper, or policy-move candidates without generating a full repo-wide report artifact."

policy:
  allow_implicit_invocation: false
