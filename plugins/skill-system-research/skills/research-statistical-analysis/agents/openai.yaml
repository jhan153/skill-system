interface:
  display_name: "Research Statistical Analysis"
  short_description: "Run design-aware, reproducible statistical analysis"
  default_prompt: "Use $research-statistical-analysis to establish the estimand and analysis unit, compute reproducibly from real data, report effects and uncertainty, and return a plan only when inputs are insufficient."

policy:
  allow_implicit_invocation: false
