interface:
  display_name: "Research Experiment Scaffold"
  short_description: "Build a minimal runnable scaffold from an approved blueprint"
  default_prompt: "Use $research-experiment-scaffold to project an approved experiment contract into repo-native code, synthetic tests, provenance, and a safe local smoke check without installing, downloading data, or training."

policy:
  allow_implicit_invocation: false
